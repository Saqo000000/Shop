@model Car
<div class="col col-lg-4">
    <img src="@Model.img" class="img-thumbnail" alt="@Model.name" />
    <h2>Model: @Model.name</h2>
    <p>@Model.shortDesc</p>
    <p>Price: @Model.price.ToString("c")</p>
    <p><a class="btn btn-warning" href="#">See more</a></p>
</div>